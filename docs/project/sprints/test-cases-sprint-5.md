# Test Cases Sprint 5 — Polish-pack + UX rename

**Дата подготовки:** 2026-05-28
**Тестируемое:** локально через `npm run dev` → `http://localhost:5173/stat-plan/`. После QA + CLOSE задеплоится на `https://cosmiksoul.github.io/stat-plan/`.
**Стратегия:** **Smoke ~10 кейсов, ~10-15 минут.** Спринт небольшой (6 polish + UX rename), большая часть логики уже покрыта unit-тестами (266 pass, +17 vs Sprint 4 FIX iter 2). Цель — убедиться что polish-эффекты видны пользователю + проверить P-7 legacy и round-trip.

---

## Как пользоваться

1. Заполняешь статусы: `ok` / `bug` / `skip` / `n/a`.
2. Если `bug` — описываешь в Bugs found ниже с severity.
3. После прохождения — заполни Резюме внизу.

**Известные concerns из code review (см. `code-review-sprint-5.md`):**

- **C-1** (minor): третья dead-ветка `baseline.unit === 'percent'` в `scoring.js` (Code missed; не блокер, cleanup в FIX или Sprint 6).
- **C-2** (minor): round-trip asymmetry для fallback case (пустой `metric_column`) — потенциально воспроизводится в кейсе 8.
- **C-3** (very minor): edge case с `metric_label: ""` (маловероятный, не покрываем смокем).
- **C-4** (эскалация): двойная локализация header `.ipynb` — `# Analysis: Тест: <название>`. **Нужно твоё решение** по формату (см. кейс 5).

---

## P-2 — CSV_PATH в скачанном `.ipynb` (1 кейс)

| #   | Test Case                                                                                                                                                          | Expected                                                                                                                                                                                                                                                                                                                | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | Заполнить бриф полностью с любым `metric_type=proportion` (быстрее всего). Утвердить план. На /step3 скачать `analysis.ipynb`. Открыть файл в текстовом редакторе. | В первой code-ячейке (после header) есть `CSV_PATH = 'experiment_results.csv'  # рядом с ноутбуком, или укажи полный путь / URL`. **Перед** code-ячейкой — markdown с инструкцией про Colab/Drive («CSV должен лежать рядом с ноутбуком, либо укажи в `CSV_PATH` полный путь / URL. Для Colab — путь в `/content/` …»). | ok     |

---

## P-3 — Filename / header / subtitle разделение (2 кейса)

| #   | Test Case                                                                                                                                                                                                                 | Expected                                                                                                                                                                                                                                                                                                                                                                                        | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2   | В брифе ввести **разные** значения «Название метрики» (натуральный текст, например `конверсия в первый депозит`) и «Колонка в CSV» (snake_case, например `cr_first_deposit`). Утвердить план. На /step3 скачать `.ipynb`. | **Имя файла:** `cr_first_deposit-v1_analysis.ipynb` (slug из `metric_column`, не из натурального текста). В файле:<br/>— Первая markdown-ячейка (header) первая строка: `# Analysis: Тест: конверсия в первый депозит` *(натуральный label, см. кейс 5 про формат);*<br/>— Subtitle: `> Test plan: см. test_plan.md, генерируется отдельно в stat·plan (шаг 2).` — **без** фразы «рядом лежит». | ok     |
| 3   | Скачать `test_plan.md` для того же state (на /step2 «↓ Скачать»). Открыть в редакторе.                                                                                                                                    | В YAML frontmatter:<br/>— `test_id: cr_first_deposit-v1` (slug из `metric_column`)<br/>— `metric_name: cr_first_deposit` (код колонки)<br/>— `metric_label: конверсия в первый депозит` (натуральный)                                                                                                                                                                                           | ok     |

---

## P-4 — Inline approx-info на Q03/Q07 (2 кейса)

| #   | Test Case                                                                                                                                                                                       | Expected                                                                                                                                                                          | Status |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 4   | На /step1 — Q03 «Тип метрики». Поочерёдно выбрать: `proportion` → `continuous` → `ratio` → `count`. После каждого выбора оставаться на вопросе несколько секунд.                                | Info-блок (нейтральный серый, начинается с `ⓘ Для точного sample size нужны параметры…`) виден **только** при `ratio` и `continuous`. При `proportion` и `count` — блока **нет**. | ok     |
| 5   | На /step1 — выбрать `ratio` или `continuous` в Q03, заполнить Q04-Q06, дойти до Q07 «MDE». Затем вернуться на Q03 (через карту вопросов), выбрать `proportion` или `count`, снова дойти до Q07. | На Q07 при `ratio`/`continuous` info-блок виден. При `proportion`/`count` — info-блока **нет**.                                                                                   | ok     |

---

## P-7 — Legacy YAML upload (1 кейс)

Подготовь файл `legacy_plan.md` руками — самый простой способ: скачай сейчас валидный план (через бриф + step 2), открой в редакторе, **удали** строку `metric_label: …` и измени `metric_name: cr_xxx` на `metric_name: "конверсия в первый депозит"`. Сохрани.

| #   | Test Case                                                                         | Expected                                                                                                                                                                                                                                                                                                        | Status |
| --- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 6   | Drag-drop `legacy_plan.md` на стартовом экране (или через file picker на /step2). | После загрузки:<br/>— ParseWarningsBanner виден с warning «Обнаружен legacy формат test_plan.md (metric_name содержит натуральный текст). Перенесено в metric_label. Заполни код колонки вручную.»<br/>— В брифе Q04: поле «Название метрики» = `конверсия в первый депозит`, поле «Колонка в CSV» = **пусто**. | ok     |

---

## UX-RENAME (1 кейс)

| #   | Test Case                                        | Expected                                                                                                                                                  | Status |
| --- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 7   | На любой странице посмотреть на Stepper в шапке. | Шаги 04 и 05 имеют новые labels: **«04 Быстрая валидация»** и **«05 Скачать артефакты»**. Оба locked (серые), кликами не открываются. Если открыты — баг. | ok     |

---

## Round-trip (регрессия после P-3+P-6+P-7, 2 кейса)

| #   | Test Case                                                                                                                                                                                               | Expected                                                                                                                                                                                                                                                                                                                                                                                                    | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 8   | Свежий бриф с **заполненными обоими полями** Q04 (natural + column). Утвердить. На /step2 скачать `test_plan.md`. Нажать «↺ Начать сначала» → подтвердить. Drag-drop того же файла на стартовом экране. | Бриф восстановлен идентично:<br/>— Q04 «Название» = тот же натуральный текст;<br/>— Q04 «Колонка в CSV» = тот же snake_case;<br/>— Никаких legacy warning'ов;<br/>— **(тест C-2 asymmetry):** если опционально оставишь Q04 «Колонка в CSV» пустым (только натуральный текст) → после round-trip в Q04 «Колонка в CSV» появится натуральный текст (полу-сломано). Это known concern C-2 — флагнуть для FIX. | ok     |
| 9   | На /step3 скачать `.ipynb`. Скачать `test_plan.md`. Очистить state. Загрузить `test_plan.md`. Заново скачать `.ipynb`. Сравнить две версии (можно diff'ом или визуально по header'у + 2-3 ячейкам).     | Файлы должны быть **идентичны** (или отличаться только timestamp'ом `Сгенерировано stat·plan YYYY-MM-DD` в header'е — если QA в разные дни).                                                                                                                                                                                                                                                                | ok     |

---

## Сборка — оценка визуально (1 кейс)

| #   | Test Case                                                                                                    | Expected                                                                                                                      | Status |
| --- | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ------ |
| 10  | `npm run build` локально. Открыть `dist/index.html` (или просто посмотреть размер `dist/assets/index-*.js`). | Сборка чистая, без warning'ов. Bundle ≤ 402 KB raw / 125 KB gzip (был 399.80 / 124.32 до Sprint 5, прирост допустим ≤ +2 KB). | ok     |

---

## Bugs found

> Заполняется во время QA. Severity: **HIGH** (блокирует деплой) / **MEDIUM** (надо в FIX iter 1) / **LOW** (можно отложить).

| # | Severity | Что не работает | Где (page/component) | Reproduction steps | Suspected cause |
|---|---|---|---|---|---|
| | | | | | |

---

## Эскалации (для пользователя)

**E-1: формат `.ipynb` header'а (C-4 из code review).** Сейчас: `# Analysis: Тест: конверсия в первый депозит` — двойной prefix (англ. + русск.). Выбери предпочтительный формат:

- (а) `# Analysis: конверсия в первый депозит` — англ. префикс + натуральный текст
- (б) `# Тест: конверсия в первый депозит` — только русск. (как title в YAML)
- (в) `# Analysis: Тест: конверсия в первый депозит` — оставить как сейчас
- (г) свой вариант

Решение запишем в Sprint 5 FIX или в CLOSE.

---

## Резюме QA

**Дата прохождения:** ___
**Время:** ___ мин
**Кейсов:** 10 / pass: ___ / bug: ___ / skip: ___

**Решения по эскалациям:**
- E-1 (формат header): ___

**Решение по C-1..C-3 (concerns из code review):** ___ (включить в Sprint 5 FIX iter 1 / отложить в Sprint 6 как первый коммит / отдельный trivial cleanup-PR).

**Следующая фаза:**
- Если bugs == 0 + решения по эскалациям приняты → переход в CLOSE Sprint 5 (Cowork-зона: FLOW/concept/JTBD §7-§8 rewrite + close tech debt).
- Если bugs > 0 → Sprint 5 FIX iter 1 prompt (с включением C-1..C-3 если решено).
